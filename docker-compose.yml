services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: todo-dashboard-frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    restart: unless-stopped
